export const selectProfileData = (state: any) => {
    return state.profileData.profileData;
}

export const selectPostData = (state: any) => {
    return state.profileData.postData;
}

export const selectStatus = (state: any) => {
    return state.profileData.status;
}